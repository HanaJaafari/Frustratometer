# readthedocs.yml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.8"

python:
  install:
    - method: pip
      path: .

conda:
  environment: docs/requirements.yaml

sphinx:
  configuration: docs/conf.py